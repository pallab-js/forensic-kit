// SPEC: REQ-000 — ForensicKit library root; exports all public API
// Phase 1 will replace this with real model + protocol definitions.

/// ForensicKit — macOS forensic data collection framework.
/// Built with Swift Package Manager. No Xcode required.
public enum ForensicKit {
    /// Semantic version of the library.
    public static let version = "0.0.0-scaffold"
}
